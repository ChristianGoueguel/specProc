% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_fit.R
\name{plot_fit}
\alias{plot_fit}
\title{Plotting of Fitted Spectral Line}
\usage{
plot_fit(
  data,
  title = NULL,
  pt.size = 3,
  pt.colour = "black",
  pt.shape = 21,
  pt.fill = "black",
  line.size = 1,
  line.colour = "red",
  linetype = "solid",
  resid.shape = 21,
  resid.size = 2,
  resid.fill = "blue",
  resid.colour = "black"
)
}
\arguments{
\item{data}{Data to be displayed from the \code{peak_fit} or \code{multipeak_fit} function}

\item{title}{Plot title}

\item{pt.size}{Size of original data point}

\item{pt.colour}{Colour of original data points}

\item{pt.shape}{Shape of original data points}

\item{pt.fill}{Colour fill of original data points}

\item{line.size}{Size of the fitted data line}

\item{line.colour}{Colour of the fitted data line}

\item{linetype}{Type of the fitted data line}

\item{resid.shape}{Shape of the residuals data points}

\item{resid.size}{Size of the residuals data points}

\item{resid.fill}{Colour fill of the residuals data points}

\item{resid.colour}{Colour of the residuals data points}
}
\value{
A \code{patchwork} (ggplot2) object.
}
\description{
Plots the data and the fitted lineshape returned by \code{\link[=peak_fit]{peak_fit()}} or
\code{\link[=multipeak_fit]{multipeak_fit()}}, together with the residuals. For multi-peak fits, the
individual peak contributions are drawn as dashed lines. When several
spectra were fitted, one panel is drawn per spectrum.
}
\examples{
\donttest{
if (requireNamespace("patchwork", quietly = TRUE)) {
  wl <- seq(395, 397, by = 0.02)
  set.seed(1)
  spec <- 10 + gaussian_profile(wl, y0 = 0, xc = 396.15, wG = 0.2, A = 50) + rnorm(length(wl))
  df <- as.data.frame(t(spec))
  names(df) <- wl
  plot_fit(peak_fit(df, profile = "gaussian"))
}
}

}
\author{
Christian L. Goueguel
}
